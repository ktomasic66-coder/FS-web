/// <reference types="node" />
export declare class PromiseSocket {
    private _attempts;
    private _timeout;
    private _socket;
    constructor(_attempts: number, _timeout: number | number[]);
    send(buffer: Buffer, host: string, port: number): Promise<Buffer>;
    closeSocket(): void;
    private _socketSend;
}
